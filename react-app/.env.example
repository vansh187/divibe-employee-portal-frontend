# "mock" runs against the in-browser MSW mock backend (localStorage-persisted seed data).
# "live" calls the real Divine Vision Employee Portal API.
VITE_API_MODE=mock

# Only used when VITE_API_MODE=live. Ask the backend team for the current dev URL.
VITE_API_BASE_URL=http://localhost:8000/api/v1
